module.exports = {
  i18n: {
    locales: ["en", "mk"], // List all supported locales
    defaultLocale: "mk", // Set the default locale
    localeDetection: true, // Enable/disable automatic locale detection
  },
  // Remove `localePath` to avoid looking for static files
};
